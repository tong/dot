#!/bin/sh

XRES="${XDG_CONFIG_HOME:-$HOME/.config}/x11/xresources"
[ -f "$XRES" ] && xrdb -merge -I"$HOME" "$XRES" &

XMODMAP="${XDG_CONFIG_HOME:-$HOME/.config}/x11/xmodmap"
[ -f "$XMODMAP" ] && xmodmap "$XMODMAP" &

#XCOLORVARS="${XDG_CONFIG_HOME:-$HOME/.config}/x11/xcolorvars"
#[ -f "$XCOLORVARS" ] && source "$XCOLORVARS" &

# xset +fp $HOME/.local/share/fonts
# xset fp rehash

#"$HOME/.local/share/screenlayout/2" &

picom -b --config "$HOME/.config/picom.conf" &

hsetroot -solid '#101010' &
#xwallpaper --zoom ~/.local/share/wallpapers/space1.jpg

xmodmap ~/.config/x11/xmodmap &
xset r rate 190 90 &
unclutter &

#autostart="mpd dunst unclutter pipewire"
#autostart="sxhkd"
# autostart="pipewire"
# for program in $autostart; do
#     pidof -s "$program" || "$program" &
# done >/dev/null 2>&1

[ -n "$xrdbpid" ] && wait "$xrdbpid"

