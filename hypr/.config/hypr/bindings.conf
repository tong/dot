# vim: filetype=conf

$mod = SUPER

#$terminal = uwsm app -- $TERMINAL
$terminal = $TERMINAL
$launcher = hyprlauncher

$resize_h = 200
$resize_v = 200

bind = $mod, F, fullscreen, 0
bind = $mod, Q, killactive,
bind = $mod, W, killactive,

bind = $mod, return, exec, $terminal
bind = $mod, B, exec, $BROWSER
bind = $mod, N, exec, $FILEMANAGER_GUI

bind = $mod, D, exec, $launcher
bind = $mod, C, exec, cliphist list | $launcher -m
# bind = $mod, C, exec, walker -m calc
# bind = $mod, D, exec, walker
# bind = $mod, S, exec, walker -m symbols
# bind = $mod, Y, exec, walker -m clipboard

bind = $mod CTRL ALT, Q, exit,

bind = $mod, H, movefocus, l
bind = $mod, J, movefocus, d
bind = $mod, K, movefocus, u
bind = $mod, L, movefocus, r

bind = $mod CTRL, H, movewindow, l
bind = $mod CTRL, J, movewindow, d
bind = $mod CTRL, K, movewindow, u
bind = $mod CTRL, L, movewindow, r

bind = $mod CTRL SHIFT, H, movetoworkspace, e-1
bind = $mod CTRL SHIFT, L, movetoworkspace, e+1

bind = $mod SHIFT, H, resizeactive, -$resize_h 0
bind = $mod SHIFT, J, resizeactive, 0 $resize_v
bind = $mod SHIFT, K, resizeactive, 0 -$resize_v
bind = $mod SHIFT, L, resizeactive, $resize_h 0
bind = $mod ALT, H, resizeactive, -$resize_h 0
bind = $mod ALT, J, resizeactive, 0 $resize_v
bind = $mod ALT, K, resizeactive, 0 -$resize_v
bind = $mod ALT, L, resizeactive, $resize_h 0

bind = $mod, tab, workspace, e-1
bind = $mod, apostrophe, workspace, e+1
# bind = $mod, escape, workspace, e-1
# bind = $mod, backspace, workspace, e+1
# bind = $mod SHIFT, tab, cyclenext
# bind = $mod SHIFT, Tab, workspace, e+1

# bind = $mod, semicolon, togglespecialworkspace
bind = $mod, i, togglespecialworkspace
bind = $mod SHIFT, i, movetoworkspace, special
# bind = $mod SHIFT, semicolon, movetoworkspace, special

#bind = $mod, P, pseudo, # dwindle
bind = $mod, T, togglesplit, # dwindle
bind = $mod, semicolon, togglefloating,

#bind = $mod, s, exec, scratchpad # move current window to scratchpad
#bind = $mod SHIFT, s, exec, scratchpad -g # move scratchpad window back to normal
#bind = $mod CTRL, s, exec, scratchpad -t

bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod SHIFT, 1, movetoworkspacesilent, 1
bind = $mod SHIFT, 2, movetoworkspacesilent, 2
bind = $mod SHIFT, 3, movetoworkspacesilent, 3
bind = $mod SHIFT, 4, movetoworkspacesilent, 4
bind = $mod SHIFT, 5, movetoworkspacesilent, 5
bind = $mod SHIFT, 6, movetoworkspacesilent, 6
bind = $mod SHIFT, 7, movetoworkspacesilent, 7
bind = $mod SHIFT, 8, movetoworkspacesilent, 8
bind = $mod SHIFT, 9, movetoworkspacesilent, 9
bind = $mod SHIFT, 0, movetoworkspacesilent, 10

bind = $mod CTRL SHIFT, 1, movetoworkspace, 1
bind = $mod CTRL SHIFT, 2, movetoworkspace, 2
bind = $mod CTRL SHIFT, 3, movetoworkspace, 3 
bind = $mod CTRL SHIFT, 4, movetoworkspace, 1
bind = $mod CTRL SHIFT, 5, movetoworkspace, 5
bind = $mod CTRL SHIFT, 6, movetoworkspace, 6
bind = $mod CTRL SHIFT, 7, movetoworkspace, 7
bind = $mod CTRL SHIFT, 8, movetoworkspace, 8
bind = $mod CTRL SHIFT, 9, movetoworkspace, 9 
bind = $mod CTRL SHIFT, 0, movetoworkspace, 10

#b# $osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"
# bindeld = ,XF86AudioRaiseVolume, Volume up, exec, $osdclient --output-volume raise
# bindeld = ,XF86AudioLowerVolume, Volume down, exec, $osdclient --output-volume lower
# bindeld = ,XF86AudioMute, Mute, exec, $osdclient --output-volume mute-toggle
# bindeld = ,XF86AudioMicMute, Mute microphone, exec, $osdclient --input-volume mute-toggle
# bindeld = ,XF86MonBrightnessUp, Brightness up, exec, $osdclient --brightness raise
# bindeld = ,XF86MonBrightnessDown, Brightness down, exec, $osdclient --brightness lower
# bindeld = CTRL, XF86AudioRaiseVolume, Volume up precise, exec, $osdclient --output-volume +1
# bindeld = CTRL, XF86AudioLowerVolume, Volume down precise, exec, $osdclient --output-volume -1
# bindeld = CTRL, XF86MonBrightnessUp, Brightness up precise, exec, $osdclient --brightness +1
# bindeld = CTRL, XF86MonBrightnessDown, Brightness down precise, exec, $osdclient --brightness -1

# bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +3% && wp-vol
# bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -3% && wp-vol
bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +3%
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -3%
bind = CTRL, XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = CTRL, XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind =, XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind =, XF86AudioPlay, exec, playerctl play
bind =, XF86AudioStop, exec, playerctl stop
bind =, XF86AudioPause, exec, playerctl pause
bind =, XF86AudioNext, exec, playerctl next
bind =, XF86AudioPrev, exec, playerctl previous
#XF86MonBrightnessUp
#XF86MonBrightnessDown

# Screenshot submap
# TODO
# bind = $mod SHIFT, S, submap, screenshot
# submap = screenshot
# #bind = , space, exec, screenshot a
# #bind = , space, submap, reset
# bindn = , a, exec, notify-send "yyyyess"
# bindn = , r, exec, /home/tong/.local/bin/screenshot r
# #bind = , r, submap, reset
# bind = , escape, submap, reset
# submap = reset

# Mediaplayer submap
bind = $mod SHIFT, P, submap, mediaplayer
submap = mediaplayer
binde = , j, exec, playerctl position 10-
binde = , k, exec, playerctl position 10+
bind = , h, exec, playerctl next
bind = , l, exec, playerctl previous
bind = , space, exec, playerctl play-pause
bind = , p, exec, playerctl play-pause
#bind = , p, submap, reset # reset submap on toggle play/pause
bind = , escape, submap, reset
submap = reset

bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
bind = $mod CTRL, mouse_down, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float * 1.1')
bind = $mod CTRL, mouse_up, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '(.float * 0.9) | if . < 1 then 1 else . end')
binde = $mod CTRL, equal, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float * 1.1')
# binde = $mod, KP_ADD, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float * 1.1')
# binde = $mod, KP_SUBTRACT, exec, hyprctl -q keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '(.float * 0.9) | if . < 1 then 1 else . end')
