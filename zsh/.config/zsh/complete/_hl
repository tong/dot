#compdef hl

# Zsh completion for HashLink (hl)
_arguments -s \
  '--debug[Start debugger on given port]:port number:_ports' \
  '--debug-wait[Wait for debugger to attach before starting]' \
  ':hl file:_files -g "*.hl"'
